# Production live verification (sanitized)

Date: 2026-09-10
Production URL: https://semantic-setoff.vercel.app/
Current source commit: c52f58683a4719035726c6fe9d917bf25beb0659
Vercel deployment: https://vercel.com/duckys-projects-bc83c6a0/semantic-setoff/GvLg2RY9jtLHjg2ie7NjLL3Mbnor
Active contract: https://explorer-studio.genlayer.com/address/0x2809483C6338861774e0D7655B1a6E33f0e9225A

HTTP/body check (PowerShell):

```text
HTTP_STATUS=200
HAS_SEMANTICSETTLEOFF=True
HAS_ROOT_ID=True
```

The production JavaScript bundle check returned the corrected contract address
and did not contain the superseded review-revision address:

```text
bundle=/assets/index-BqaUIz5X.js
HAS_CORRECT_ADDRESS=True
HAS_SUPERSEDED_ADDRESS=False
```

Browser-local IC RPC check from the production origin:

```text
POST https://studio.genlayer.com/api
method=eth_chainId
status=200
body={"jsonrpc":"2.0","result":"0xf22f","id":9}
```

The browser result `0xf22f` is chain `61999` (Studionet). A Chrome session
detected OKX Wallet, connected account `0xbd733bc56ec4a55fa25c068b9306b0171335d199`,
created `round-mtse8le6`, and the canonical read after the wallet transaction
reported `state=OPEN`, `fundedCount=1`, `totalFundedGEN=2`, and
`lockedFundsGEN=2`. The wallet transaction hash is recorded in
`docs/evidence/studionet/browser-wallet-funding.json`.

Two authorized script-signed demo actors then joined the same round with 2 GEN
each (`FINALIZED/MAJORITY_AGREE`), and a fresh Chrome canonical read reported
`state=FUNDED`, `fundedCount=3`, `totalFundedGEN=6`, and `lockedFundsGEN=6`.

Direct production route checks returned the intended app for `/`, `/rounds`,
`/rounds/new`, `/rounds/:roundId`, `/rounds/:roundId/obligations/new`,
`/rounds/:roundId/review`, `/activity`, `/settings`, `/help`, and the not-found
route. Console error/warning count for the clean live browser run was zero.

The Chrome wallet extension itself requires the user to confirm signing; the
agent did not access or automate the `chrome-extension://` popup. The script
signed Studionet lifecycle remains separate from this browser-wallet proof.

Fresh Chrome read-only verification of the production app showed the home page,
then `/settings` with `Transaction network=Studionet`, `Target chain=61999`,
`Canonical read path=Configured`, `Writes enabled=Yes`, and two detected wallet
providers. Browser console error/warning count was zero for this run.

After the wallet-session regression fix, the same Chrome tab connected the OKX
account, reloaded `/rounds`, restored `0xbd73…d199` without reopening the wallet
picker, and then loaded the canonical `round-mtux7c77` state. The round's browser
payment was finalized with 2 GEN locked (`0x763b6f89723fd6f4efab62d9baf30317cbe77441abf013a14823b56013a0a33c`).

The same tab then opened the account menu, selected `Disconnect`, and reloaded
`/rounds`; the result remained `Connect wallet` with no automatic reconnection.
