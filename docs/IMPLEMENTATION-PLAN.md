# SemanticSetoff end-to-end execution plan

Date: 2026-09-08  
Category lock: `Projects`  
Target network: Studionet  
Submission channel: Portal Builders  
Current checkpoint: reviewer-requested charter evidence binding,
charter-grounded verdict rules, deterministic ambiguity precedence, and the
frontend 600-character limit pass locally and in a corrected Studionet
deployment/lifecycle. Production frontend publishing remains next.

This plan operationalizes `MASTER-PROMPT-GENLAYER-END-TO-END.md` without
replacing it. A phase starts only after the prior exit gate is supported by
fresh output. Any source/authenticity, equivalence, settlement-invariant,
accounting, browser-RPC, deployment, or public-hygiene blocker returns work to
the named earlier phase instead of narrowing tests or inflating claims.

## Global acceptance ledger

| Requirement | Planned proof |
|---|---|
| Exactly 14 gates | Registry/spec parser reports 14 named rows and all `PASS` |
| Projects category | Spec, package metadata, README, registry and acceptance command agree |
| Contract source | ASCII scan, exact Depends header, exactly one project-specific `gl.Contract` subclass, current `genvm-lint check` |
| Value | UI/docs use GEN; contract/tests convert with `10**18`; demo writes use only 1 or 2 GEN |
| Frontend | Nine named routes, persistent navigation, wallet picker/logout, typed adapter, role/state actions, finality states, responsive browser checks |
| Local quality | Root `npm run check` runs lint, direct tests, parser/frontend tests, TypeScript and production build with zero critical skips |
| Network | Allowlisted Studionet deployment/lifecycle evidence: success, accepted/finalized states, canonical before/after reads, transaction URLs and GEN accounting |
| Publication | Exact public allowlist, secret/history scan, GitHub public repo, CI for current commit |
| Live app | Production Vercel URL, HTTP 200, app name and React root, browser-local read/write-path checks |
| Submission | `Project semantic-setoff -Category projects` reports no blocker; copy-ready packet and exact five-line report use verified URLs only |
| Final audit | Master prompt re-read in full; every phase/directive/checklist item reconciled as DONE or explicitly uncertain |

## Phase-by-phase plan

### Phase 0 — Recover and protect the workspace — DONE

- Treat `D:\Genlayer Project` as the non-Git knowledge root.
- Preserve all existing dirty child repositories.
- Permit edits only to root `docs/IDEA-REGISTRY.md` and the new
  `semantic-setoff` child repository.
- Discover credentials by presence only; never print values.
- Proof: root Git check, child repo inventory/status, `.env` variable-name-only
  inspection, GitHub/Vercel authentication checks.

### Phase 1 — Research ecosystem and rules — DONE

- Read the master prompt and full authority chain.
- Use official GenLayer documentation for when-to-use, current network, runner
  and SDK/tool versions; separate current main docs from the coherent stable
  v0.2 runner/API family.
- Compare all registry entries and public agent-protocol landscape.
- Proof: recorded official source list, version command output and collision
  notes. Do not claim web research as runtime proof.

### Phase 2 — Diverge, gate and select — DONE

- Screen ten problem seeds and three structurally different survivors.
- Reject ScopeHandshake at Differentiation rather than renaming an existing
  policy-quorum primitive.
- Select SemanticSetoff only after all 14 gates, Evidence Authority Matrix,
  three reuse consumers and milestone headroom pass.
- Proof: 14 gate rows, seven fingerprint fields and four authority rows in the
  registry; selection spike labeled design-only.

### Phase 3 — Register and scaffold — DONE

- Register IDEA-026 before contract source.
- Create exactly one child repo with its own `.git`, ignored local `.env`, and
  contracts/tests/scripts/docs/evidence/frontend locations.
- Proof: `git rev-parse --show-toplevel`, `git check-ignore .env`, clean origin
  from no inherited sample.

### Phase 3A — Blueprint, skill-led design and frontend build — DONE

1. Keep the nine-route blueprint and provisional capability/action/visibility
   matrices in `docs/README.md`.
2. Use persisted `ui-ux-pro-max` source of truth: Fintech/Crypto,
   Trust & Authority, Minimalism & Swiss, dark slate + gold + purple, IBM Plex
   Sans, variance 4/motion 3/density 5.
3. Implement Vite/React routes: Home, Rounds, New Round, Round Workspace, New
   Obligation, Review/Result, Activity, Account/Network and Help.
4. Implement persistent navigation, accessible focus/keyboard behavior,
   responsive states, honest missing-configuration handling, and a read-only
   fixture mode with a permanent non-live banner.
5. Implement EIP-6963-first wallet discovery plus injected fallbacks, centered
   selected-provider modal, explicit account request, Studionet switch/add,
   account menu and disconnect.
6. Keep every write behind a typed adapter; no private key, localStorage
   canonical state or simulated success/finality.
7. Install exact dependencies, run typecheck/build and inspect 375/768/1024/
   1440 layouts plus reduced motion.
- Exit commands: `npm ci`, `npm run typecheck`, `npm run build`, route source
  inventory, static honesty/secret scan, browser screenshots and console/network
  inspection.

### Phase 3B — Frontend self-review

Status: `DONE` — visual structure is now governed by FE-PRESERVE.

1. Walk discover -> understand -> connect -> create/open -> act -> review ->
   result -> history -> withdraw/recover across real routes.
2. Confirm every route is reachable from navigation or a contextual link.
3. Confirm USER_PRIMARY/USER_CONTEXTUAL/SYSTEM_ONLY visibility, role/state
   gating, English outcome labels and complete empty/loading/error/submitted/
   accepted/finalized/retry/cancel states.
4. Confirm no fixture/static file appears canonical and no method-list console
   leaked into the primary experience.
5. Fix only blueprint mismatches; then freeze visual structure under
   FE-PRESERVE.
- Exit commands: fresh typecheck/build; browser route/a11y/console checks;
  `rg` scans for fixtures, localStorage, private keys and forbidden copy.

### Phase 4 — Lock full specification — DONE

1. Replace every Phase 4 placeholder with the full template content.
2. Freeze stable IDs, structured storage, state diagram, legal/illegal
   transitions, role authorization and direct per-entrypoint time semantics.
3. Complete the write-method safety matrix before contract writes, including
   open, ratify/fund, record, accept, review/retry, expire and withdraw.
4. Complete the value-destination matrix for all 6 GEN in SETTLED,
   NOT_NETTABLE, EXPIRED, retry and failed-withdraw paths.
5. Copy the Evidence Authority Matrix and make objective verification,
   artifact isolation, provenance tripwires and blocked consequences exact.
6. Lock nondeterministic schema, semantic validator, full-coverage/root/class/
   consequence invariants and invalid-output behavior before mutation.
7. Complete frontend lifecycle, claim-to-code, threat, test, deployment,
   differentiation, DoD and limitation matrices with no blank cells.
8. Reconcile every visible frontend action against a legal contract path;
   make only FE-PRESERVE-compatible corrections.
- Exit proof: spec lint script reports no placeholder/blank required cells,
  exact row counts and all value/actions traced; registry status becomes
  `BUILDING` only then.

### Phase 5 — Contract implementation — DONE

1. Read the current `write-contract` and `genvm-lint` skills and `docs/05`
   routing instructions before source actions.
2. Use exact stable boilerplate Depends hash with v0.2 API as one versioned
   unit; ASCII; `from genlayer import *`; exactly one `SemanticSetoff(gl.Contract)`.
3. Implement structured round/participant/obligation/credit storage and string
   public map keys without reinitializing class-body containers.
4. Use only safety-card methods and direct clock guards; exact payable metadata
   on 2 GEN funding; debit-before-transfer withdrawal.
5. Implement no-arg leader and semantic validator using D3, bounded prompts and
   exact stored content.
6. Parse/drop unknown fields, validate full expected ID coverage/enums/roots/set
   digest, derive consequence in code, then compute zero-sum credits before any
   hard-state mutation.
7. Expose only required canonical views; no fake consumer contract.
- Exit proof: `GENVM_VERSION=v0.2.16 genvm-lint check contracts/semantic_setoff.py`
  recognizes `SemanticSetoff` with 14 methods; ASCII, header and one-class
  checks pass; source-to-safety-matrix comparison is clean.

### Phase 6 — Test-first direct/local verification — DONE

1. Read `test-driven-development` and `direct-tests` skills before tests.
2. Write failing tests before each behavior cluster: auth/isolation, state,
   time boundaries, provenance, nondeterminism/invariants, accounting/recovery,
   payability and parser shapes.
3. Cover boundary-1/exact/+1 with stale phase for every temporal write and prove
   state/accounting unchanged on rejection.
4. Mock nondeterminism before transactions and test semantic replay plus
   malicious valid-shape outputs: extra/missing/duplicate IDs, invalid enum,
   false conflict root, wrong set digest and prompt injection.
5. Cover digest-correct/wrong-authority tripwires for charter, debtor and
   creditor bindings.
6. Cover 1/2 GEN units, total 6 GEN, credit ranges, refund paths, duplicate
   review/settle/expire/withdraw and failed transfer behavior.
7. Cover raw and normalized Studio receipt parsers and frontend helpers.
8. Create repo Python 3.12 `.venv`, keep `PYTHONUTF8=1`, and aggregate everything
   in root `npm run check`.
- Exit commands: targeted green direct log (`16 passed`), frontend adapter log,
  and `npm run check`; no skip/xfail and exact test count captured. The first
  red signal was a real temporal-clock mismatch; switching `_now()` to the
  GenVM-controlled `datetime.now(timezone.utc)` resolved it and the full suite
  was rerun.

### Phase 7 — Preserve design and wire real genlayer-js — DONE

1. Add current `genlayer-js` without changing page/layout/tokens.
2. Implement the real adapter with `createClient({ chain: studionet,
   account: userAddress })`; never per-call raw-string account overrides.
3. Keep wallet traffic on selected EIP-1193 provider and IC reads on configured
   IC RPC or same-origin `/genlayer-rpc` proxy.
4. Map every canonical view/status to existing UI; calculate action visibility
   from role + state + clock.
5. Implement submitted -> accepted/decided -> finalized polling and mandatory
   canonical reload; retain failed/retryable truth.
6. Add focused tests for provider choice, no auto-connect, disconnect, chain
   switch/add, separate RPC paths, adapter mapping, action gating, finality and
   reload.
7. Run a browser-local CORS/fetch test against configured read/write paths.
- Exit proof: `frontend/npm test`, TypeScript, and production build pass;
  `LiveContractAdapter` uses configured-account `createClient`, separate IC
  read endpoint/proxy, accepted/finalized polling, canonical reload and
  retryable ambiguity; absent-address mode remains honest and every claimed
  lifecycle row has a wrapper.

### Phase 8 — Studionet deploy and consequential lifecycle — DONE

1. Read `genlayer-cli` and `integration-tests` skills; check current official
   network/tool status before diagnosing failures.
2. Safely discover project then authorized parent `.env` by presence only.
3. Require three distinct authorized actor addresses for the product; if this
   cannot be achieved without creating/funding new EOAs, stop for the explicit
   authority required by AGENTS.md.
4. Build resumable/idempotent inspect/deploy/demo scripts and bind network,
   source commit, runner/API family, actor addresses and contract address.
5. Deploy once, require `Result: SUCCESS`, and project receipt allowlist only.
6. Run open -> three 2 GEN ratifications -> three 1/2 GEN obligations -> three
   creditor acceptances -> review -> accepted/finalized -> canonical settlement
   -> withdrawal, plus a conflict/refund or expiry lifecycle when safe.
7. Record safe transaction hashes, Explorer URLs, actors, timestamps,
   before/after views and GEN balance deltas under Studionet evidence only.
- Exit proof: finalized consequential lifecycle and zero/fully accounted
  contract state; otherwise mark the exact item `PENDING_REAL_EVIDENCE` and do
  not widen claims. Current safe preflight output is
  Preflight reached `ACTORS_READY count=3`. Deployment is bound in
  `docs/evidence/studionet/deployment.json`; review-revision lifecycle round
  `setoff-a64deef1` reached `SETTLED`, all 12 transactions were
  `FINALIZED/MAJORITY_AGREE/SUCCESS`, and canonical accounting holds with 6
  GEN withdrawn. Superseded deployments are separately archived.
  Local gltest smoke remains `PENDING_HARNESS_FIX` in
  `docs/evidence/local/gltest-smoke.txt`.

### Phase 9 — Bind deployed address — DONE LOCALLY

- Put only public deployment/RPC configuration in ignored `frontend/.env`.
- Rebuild and ensure the live adapter reads the active deployment.
- Exit command: `cd frontend; npm run build` with the active Studionet address
  in ignored `frontend/.env`; build succeeds.

### Phase 10 — English-only audit — DONE

- Scan frontend, root README and code comments for non-English user copy.
- Preserve technical enums/keys/URLs.
- Exit proof: scan output plus manual review reports no non-English user-facing
  string.

### Phase 11 — Public GitHub hygiene and publication — DONE

1. Create a public-file allowlist and inspect top-level, status, diff, staged
   list and tracked list.
2. Scan staged content and entire history for env files, keys, wallet exports,
   root control documents, prompts, caches, evidence dumps and inherited
   boilerplate.
3. Verify ignored local `.env` files still exist and remain ignored.
4. Commit in logical checkpoints, create public `semantic-setoff` repo under
   the authenticated account, set one-sentence description and push `main`
   without force.
5. Add CI using the same `npm run check`; verify the run belongs to current
   commit.
- Exit proof: public URL, intact history, allowlisted `git ls-files`, secret scan
  clean and successful current-commit CI.

### Phase 12 — Vercel production deploy — DONE

- Create/import project with repo name, root `frontend`, Vite preset,
  `npm run build`, output `dist`, and public production env only.
- Try exact subdomain then `-app`, `-dapp`, `-demo` in order if occupied.
- Exit proof: `https://semantic-setoff.vercel.app/` returns a READY production
  deployment with public configuration and the SPA rewrite.

### Phase 13 — Live verification — DONE (browser write scope documented)

- Require `curl -I` HTTP 200 and first 4,000 body bytes containing
  `SemanticSetoff` and `id="root"`.
- Open the real production URL in a browser and inspect every route, responsive
  layout, console, IC reads, wallet picker and at least the safely authorized
  browser transaction lifecycle. Distinguish read proof from write proof.
- Exit proof: `docs/evidence/live/app.txt` plus
  `docs/evidence/studionet/browser-wallet-funding.json`. The extension popup is
  user-controlled; the recorded browser write finalized and canonical state
  reloaded. No wallet key or extension internals were accessed.

### Phase 14 — Final README and push — DONE

- Write a public root README with pitch, trust boundary, architecture, methods,
  exact tests, Studionet steps/evidence, contract/Explorer, live app, local run,
  security and honest limitations.
- Push a new commit only after hygiene checks repeat.
- Exit proof: remote README at the pushed commit contains the verified live URL,
  public repository, and active contract address.

### Phase 15 — Four-source audit, acceptance gate and submission packet — DONE

1. Cross-check source, fresh test output, current network evidence and all
   README/docs/frontend claims.
2. Reconcile exact contract/test counts, zero critical skips, all value/recovery
   negative tests, every settlement invariant, every authority row and every
   claimed browser action.
3. Run the workspace acceptance tool exactly for this repo/category with public
   repo and primary Explorer evidence; require `NO BLOCKER`.
4. Prepare Projects title, <=1,000-character English description with exact
   count, repository/contract/lifecycle/CI/demo evidence, verified facts,
   limitations and category rationale.
5. Do not submit through the authenticated Portal session in this run: final
   Submit requires separate action-time authorization. The packet is copy-ready.
6. Emit the exact five-field short report only with verified URLs/address.
- Exit proof: clean audit, `NO BLOCKER` acceptance output and self-contained
  packet; Portal final submission is the one intentionally pending action.

### Phase 16 — Postmortem and root registry truth — DONE (Portal pending)

- Record what passed, failed, required redesign and remains pending; update
  IDEA-026 status/fingerprint/outcome without overstating Portal acceptance.
- Record next substantial milestone: authenticated A2A/AP2 evidence adapter;
  retain multi-party residual-cycle and consumer gateway as later options.
- Add lessons to the project postmortem and registry so future ideas do not
  repeat SemanticSetoff or the rejected ScopeHandshake shape.
- Exit proof: registry status matches current public/network/Portal facts and
  names the next milestone opportunity.

## Final re-read and closure procedure

1. Re-read the full master prompt from line 1 through `PROMPT END`.
2. Re-run a checklist extractor and reconcile Phases 0, 1, 2, 3, 3A, 3B and
   4–16; all 14 gates; FE-PRESERVE, FE-HONEST, FE-WALLET-EVM,
   FE-WALLET-ACCOUNT, FE-SURFACE and FE-PRODUCT.
3. Map every DONE item to a command plus real output/evidence path.
4. List any uncertainty or unverified external fact instead of inferring it.
5. Run final `npm run check`, public hygiene/secret scan, live HTTP/body checks,
   current deployment reads and the objective acceptance command.
6. Only then report completion; otherwise report the exact earliest incomplete
   exit gate and keep later claims pending.
